# agentic-day1

# Day 1 Assignment: Context Failure to Context Fix

## Objective

This project demonstrates why naive string-based LLM calls can lose context and how structured message history fixes the issue.

## Setup

Install dependencies:

```bash
pip install -r requirements.txt
```
